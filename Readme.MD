### git add . && git commit -m "adding script" && git push

```
git add . && git commit -m "adding script" && git push
```

### $? → exit status of last command
### $@ → all script arguments
### $# → number of arguments
### $* = all arguments passed to the script (as one single string)